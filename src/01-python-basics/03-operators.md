# 1.3 연산자: 더하기부터 비교까지

## 연산자가 뭐예요?

연산자는 **값과 값 사이에 끼워서 무언가를 계산하게 하는 기호**입니다. 사실 우리가 초등학교 때 다 배운 거예요.

```python
2 + 3    # 5
10 - 4   # 6
```

`+`, `-` 이런 게 다 연산자예요. 파이썬에는 크게 4가지 종류가 있습니다.

1. **산술 연산자** — 더하기, 빼기, 곱하기 등
2. **비교 연산자** — 같다, 크다, 작다
3. **논리 연산자** — and, or, not
4. **할당 연산자** — `=`, `+=` 같은 것들

이번엔 멤버십 연산자(`in`)도 잠깐 다룰게요.

---

## 1. 산술 연산자

### 기본 4종

```python
print(10 + 3)    # 13   (덧셈)
print(10 - 3)    # 7    (뺄셈)
print(10 * 3)    # 30   (곱셈)
print(10 / 3)    # 3.333... (나눗셈)
```

⚠️ **주의:** 나눗셈 `/`은 항상 **실수(float)** 를 반환합니다. `10 / 2`도 `5.0`이지 `5`가 아니에요.

### 좀 특이한 3종

```python
print(10 // 3)   # 3    (몫만)
print(10 % 3)    # 1    (나머지만)
print(10 ** 3)   # 1000 (10의 3제곱)
```

| 연산자 | 이름 | 예시 | 결과 |
|------|------|------|------|
| `//` | 몫 (정수 나눗셈) | `7 // 2` | `3` |
| `%` | 나머지 | `7 % 2` | `1` |
| `**` | 거듭제곱 | `2 ** 10` | `1024` |

### 어디에 써요?

**`%` (나머지)** 는 의외로 정말 자주 씁니다. "짝수냐 홀수냐"를 확인할 때나, "5의 배수마다 뭔가 하기" 같은 일에요.

```python
n = 14

if n % 2 == 0:
    print("짝수")
else:
    print("홀수")
```

**`**`** 는 거듭제곱이지만, 머신러닝에서는 "제곱근"을 위해 자주 씁니다.

```python
print(16 ** 0.5)    # 4.0   (16의 1/2제곱 = 제곱근)
print(8 ** (1/3))   # 2.0   (8의 1/3제곱 = 세제곱근)
```

### 연산 순서 (우선순위)

학교에서 배운 그대로예요. 곱셈/나눗셈이 덧셈/뺄셈보다 먼저, 거듭제곱이 가장 먼저.

```python
print(2 + 3 * 4)        # 14   (3*4=12 먼저)
print((2 + 3) * 4)      # 20   (괄호 먼저)
print(2 ** 3 * 4)       # 32   (2**3=8 먼저)
```

**기억 안 나면 그냥 괄호 쓰세요.** 코드 가독성도 좋아져요. 우선순위를 다 외우려고 애쓰지 마세요.

```python
# 좀 더 명확하게
result = (2 ** 3) * 4
```

---

## 2. 비교 연산자

값을 비교하면 결과는 **`True` 또는 `False`** 로 나옵니다.

```python
print(5 > 3)     # True
print(5 < 3)     # False
print(5 == 5)    # True
print(5 != 5)    # False
print(5 >= 5)    # True
print(5 <= 4)    # False
```

| 연산자 | 의미 |
|------|------|
| `==` | 같다 |
| `!=` | 다르다 |
| `>` | 크다 |
| `<` | 작다 |
| `>=` | 크거나 같다 |
| `<=` | 작거나 같다 |

⚠️ **주의:** **"같다"는 `=`이 아니라 `==` 입니다!**

`=`은 "왼쪽에 오른쪽을 담아라"라는 할당이에요. 비교는 `==`을 두 개 써야 해요. 처음에 정말 자주 헷갈리는 부분입니다.

```python
age = 25       # 할당: age라는 이름표를 25에 붙임
age == 25      # 비교: age가 25랑 같은지? → True
```

### 문자열도 비교됩니다

```python
print("apple" == "apple")    # True
print("apple" == "Apple")    # False  (대소문자 구분!)
print("apple" < "banana")    # True   (사전 순서)
```

문자열을 비교하면 **사전 순서**로 비교돼요. `apple`이 `banana`보다 사전에서 먼저 나오니까 작다고 판단합니다.

### 연쇄 비교

이건 파이썬의 예쁜 기능 중 하나예요.

```python
age = 25
if 20 <= age < 30:
    print("20대")
```

`20 <= age < 30`이 `(20 <= age) and (age < 30)`을 한 줄에 쓴 것과 같아요. 다른 언어에서는 안 되지만 파이썬에서는 됩니다.

---

## 3. 논리 연산자

`True/False` 값들끼리 결합할 때 씁니다.

| 연산자 | 의미 | 예시 |
|------|------|------|
| `and` | 둘 다 참이면 참 | `True and True` → `True` |
| `or` | 하나라도 참이면 참 | `True or False` → `True` |
| `not` | 반대로 뒤집기 | `not True` → `False` |

```python
age = 25
has_license = True

# 운전할 수 있나?
can_drive = (age >= 18) and has_license
print(can_drive)    # True

# 학생 또는 노인이면 할인
is_student = False
is_senior = True
can_discount = is_student or is_senior
print(can_discount)    # True

# 미성년자가 아니면
is_minor = age < 18
can_vote = not is_minor
print(can_vote)    # True
```

### 진리표 (외울 필요 없어요)

| A | B | A and B | A or B |
|---|---|---------|--------|
| True | True | True | True |
| True | False | False | True |
| False | True | False | True |
| False | False | False | False |

**한 줄 요약:**
- `and`: 둘 다 참이어야 참
- `or`: 하나만 참이어도 참
- `not`: 뒤집기

---

## 4. 할당 연산자

이미 알고 계신 `=`이 가장 기본 할당 연산자예요. 그런데 자주 쓰는 단축형들이 있어요.

```python
count = 0

count = count + 1    # count를 1 증가
count += 1           # 위와 똑같음 (단축형)
```

| 연산자 | 의미 |
|------|------|
| `+=` | 더한 후 할당 |
| `-=` | 뺀 후 할당 |
| `*=` | 곱한 후 할당 |
| `/=` | 나눈 후 할당 |
| `**=` | 거듭제곱 후 할당 |

```python
score = 100
score += 10     # score = score + 10  → 110
score -= 5      # 105
score *= 2      # 210
score /= 3      # 70.0
print(score)
```

머신러닝 코드에서 **누적 합** 계산할 때 자주 봅니다.

```python
total_loss = 0
for batch in batches:
    loss = compute_loss(batch)
    total_loss += loss   # ← 이런 식
```

---

## 5. 멤버십 연산자: in, not in

이건 **"안에 있는지 확인"** 하는 연산자예요. 다음 글의 자료구조에서 본격적으로 쓰지만, 지금 미리 봐도 좋아요.

```python
fruits = ["사과", "바나나", "딸기"]

print("사과" in fruits)        # True
print("포도" in fruits)        # False
print("포도" not in fruits)    # True
```

문자열 안에 특정 글자가 있는지도 확인할 수 있어요.

```python
sentence = "오늘 날씨가 좋다"
print("날씨" in sentence)    # True
print("비" in sentence)      # False
```

---

## 모두 합쳐 보기

이제 진짜 코드 같은 걸 짜 봅시다. 입장 가능 여부를 확인하는 코드예요.

```python
age = 25
ticket = True
is_member = False
points = 1500

# 입장 조건:
# 1. 나이가 19세 이상
# 2. 티켓이 있거나, 회원이거나, 포인트가 1000 이상
can_enter = (age >= 19) and (ticket or is_member or points >= 1000)

print(f"입장 가능: {can_enter}")    # 입장 가능: True
```

이 한 줄에 비교 연산자(`>=`), 논리 연산자(`and`, `or`), f-string이 다 들어 있어요.

---

## 정리: 연산자 한 페이지 요약

```python
# 산술
+   -   *   /     # 덧셈, 뺄셈, 곱셈, 나눗셈
//  %   **        # 몫, 나머지, 거듭제곱

# 비교 (결과는 True/False)
==  !=            # 같다, 다르다
>   <   >=  <=    # 크다, 작다, 이상, 이하

# 논리
and   or   not    # 둘 다, 하나라도, 뒤집기

# 할당 단축
+=  -=  *=  /=    # x = x + 1 → x += 1

# 멤버십
in    not in      # 안에 있냐, 없냐
```

---

## 자주 묻는 질문

> **Q. `=` 와 `==` 정말 매번 헷갈려요. 팁 있나요?**
>
> 이렇게 외우시면 도움 됩니다.
> - `=` 한 개 → "값을 넣어라" (할당)
> - `==` 두 개 → "값이 같냐?" (비교)
>
> 그리고 IDE/에디터들이 `if x = 5:` 같이 쓰면 빨간 줄로 알려줘요. 그것도 도움이 됩니다.

> **Q. `and`와 `&&` 차이는 뭐예요?**
>
> 자바스크립트나 C 같은 언어에서는 `&&`을 쓰지만, **파이썬은 `and`를 씁니다.** 영어 단어 그대로요. 처음에는 어색해도 금방 익숙해져요.

> **Q. `5 + 3 * 2`는 11이지만 `(5 + 3) * 2`는 16인 거 맞죠?**
>
> 맞습니다. 곱셈이 덧셈보다 먼저예요. 모든 우선순위를 외우려 하지 말고, **헷갈리면 괄호를 쓰세요.** 코드 읽기에도 더 좋습니다.

> **Q. `is`와 `==`은 뭐가 다른가요?**
>
> 좋은 질문이지만 지금 단계에서는 그냥 **`==`만 쓰세요.** `is`는 "같은 메모리 주소냐?"를 묻는 거고, `==`는 "값이 같냐?"를 묻습니다. 거의 항상 후자가 우리가 원하는 거예요. 단, **`x is None` 같은 경우는 예외**로 `is`를 씁니다 (이건 관례).

➡️ 다음: [1.4 자료구조: 데이터를 담는 그릇 4종](04-data-structure.md)

---

## 자주 하는 실수와 해결

- 실수: `=`와 `==`를 혼동한다.
    해결: `=`는 대입, `==`는 비교라는 역할 차이를 항상 문장으로 읽어 봅니다.
- 실수: 연산자 우선순위를 감으로 처리한다.
    해결: 괄호를 적극 사용해 의도를 명확히 표현합니다.
- 실수: 논리 연산 결과를 직관으로만 판단한다.
    해결: `True/False` 표를 직접 써 보고 검증합니다.

## 짧은 복습 체크리스트

- [ ] 비교/논리/산술 연산자의 역할을 구분할 수 있다.
- [ ] 조건식에서 결과가 왜 참/거짓인지 설명할 수 있다.
- [ ] 복잡한 식에 괄호를 넣어 가독성을 높일 수 있다.



<!-- THEORY_EXPANDED_2026_05 -->

---

## 이해를 더 단단하게 만드는 보강 설명

이 단원은 처음 읽을 때는 "아, 이런 개념이 있구나" 정도로 넘어가기 쉽지만, 실제로는 작은 차이가 나중에 큰 성능 차이로 이어집니다. 특히 **1.3 연산자: 더하기부터 비교까지** 같은 주제는 한 번에 완벽히 이해하려고 하기보다, 다음 세 가지를 반복하면서 이해를 깊게 만드는 편이 훨씬 효과적입니다.

첫째, 용어를 정의로 외우지 말고 상황으로 연결해 보세요. 예를 들어 "왜 이 개념이 필요한가"를 스스로 설명할 수 있어야 다음 단원에서 응용이 됩니다. 둘째, 코드가 나오면 결과를 먼저 예측한 뒤 실행해 보세요. 예측과 결과가 다를 때가 진짜 학습이 일어나는 순간입니다. 셋째, 같은 코드를 숫자나 파라미터만 바꿔 여러 번 돌려 보세요. 머신러닝과 딥러닝은 정답을 암기하는 과목이 아니라 **변화를 읽는 과목**이기 때문입니다.

또 하나 중요한 점은, 이 단원의 내용이 단독으로 존재하지 않는다는 사실입니다. 실제 프로젝트에서는 데이터 준비, 모델링, 평가, 배포가 끊기지 않고 이어집니다. 그래서 지금 배우는 **1.3 연산자: 더하기부터 비교까지**도 나중에 다른 단계와 반드시 연결됩니다. 이 연결을 의식하면서 읽으면, 단편 지식이 아니라 시스템 관점으로 이해하게 됩니다.

---

## 자주 하는 오해와 바로잡기

### 1) "이론만 이해하면 실습은 저절로 된다"
아닙니다. 이론 이해와 코드 실행 능력은 별개의 근육입니다. 둘을 같이 써야 합니다.

### 2) "예제 코드가 돌아갔으니 완전히 이해했다"
동작 확인은 시작일 뿐입니다. 파라미터를 바꿨을 때 결과가 어떻게 달라지는지까지 확인해야 이해가 완성됩니다.

### 3) "좋은 모델은 항상 하나다"
데이터 특성과 제약(시간, 메모리, 응답 속도)에 따라 최선의 선택은 달라집니다.

### 4) "지표 숫자만 높으면 끝이다"
지표는 해석이 필요합니다. 어떤 데이터에서, 어떤 분포에서, 어떤 기준으로 얻은 점수인지 함께 보아야 합니다.

### 5) "지금은 초반이니까 배포는 나중에 생각해도 된다"
초반부터 재현성과 저장 구조를 습관화해야 나중에 배포 단계에서 무너지지 않습니다.

---

## 실전 연결 시나리오

아래는 이 단원 내용을 실제 업무로 연결할 때 자주 쓰는 흐름입니다.

1. 문제를 한 문장으로 정의한다.  
예: "1.3 연산자: 더하기부터 비교까지 관점에서 입력으로부터 어떤 출력을 만들 것인가?"

2. 성공 기준을 지표로 정한다.  
예: 정확도/RMSE/F1/지연시간 같은 운영 가능한 숫자로 정의.

3. 가장 단순한 베이스라인을 만든다.  
복잡한 모델보다 먼저, 빠르게 재현 가능한 기본 모델을 세웁니다.

4. 한 번에 하나씩 개선한다.  
전처리, 모델, 튜닝, 임계값, 배치 크기 등은 동시에 바꾸지 않고 순차적으로 바꿉니다.

5. 결과를 로그와 함께 저장한다.  
실험 이름, 파라미터, 점수, 데이터 버전을 함께 남겨야 재현이 됩니다.

6. 실패 케이스를 분석한다.  
점수 평균보다 오분류/고오차 샘플을 분석할 때 개선 아이디어가 나옵니다.

7. 배포 가능 형태로 정리한다.  
모델 파일, 메타데이터, 입력 스키마, 테스트 스크립트까지 묶습니다.

---

## 복습 체크리스트

다음 질문에 답할 수 있으면 이 단원을 실전 수준으로 이해한 것입니다.

- 이 단원의 핵심 개념을 중학생에게 설명하듯 말할 수 있는가?
- 코드의 각 블록이 왜 필요한지 한 줄씩 설명할 수 있는가?
- 이 단원에서 가장 자주 틀리는 지점을 알고 있는가?
- 지표를 볼 때 어떤 함정이 있는지 알고 있는가?
- 베이스라인과 개선 모델의 차이를 표로 정리할 수 있는가?
- 재현 가능하게 실험을 저장하는 습관이 있는가?
- 실패 사례를 보고 다음 실험 가설을 만들 수 있는가?
- 이 단원 내용을 다음 장과 어떻게 연결할지 설명할 수 있는가?

---

## 확장 연습 과제

### 과제 A: 파라미터 감각 만들기
현재 예제에서 핵심 파라미터 1~2개만 바꿔 5회 실험하고, 결과를 표로 정리해 보세요.

### 과제 B: 실패 사례 분석
오분류 또는 오차가 큰 샘플 20개를 모아 공통 패턴을 찾아 보세요.

### 과제 C: 설명 가능 리포트 작성
"무엇을 했고, 왜 했고, 결과가 어땠고, 다음엔 무엇을 할지"를 1페이지로 정리해 보세요.

이 3개를 직접 해 보면 **1.3 연산자: 더하기부터 비교까지**의 이해도는 읽기만 했을 때보다 훨씬 빠르게 올라갑니다.

---

## 한 번 더 정리

1.3 연산자: 더하기부터 비교까지는 단순히 시험용 개념이 아니라, 실제 문제를 풀 때 반복해서 쓰게 되는 실전 도구입니다. 처음에는 낯설 수 있지만, 작은 실험을 반복하면 감각이 생기고, 감각이 생기면 코드와 지표를 읽는 속도가 급격히 빨라집니다. 지금 단계에서 중요한 것은 완벽한 암기가 아니라, **작게 실행하고, 결과를 읽고, 다시 수정하는 루프**를 몸에 익히는 것입니다. 이 루프를 익히면 다음 장으로 넘어갈수록 학습 속도가 오히려 빨라집니다.
